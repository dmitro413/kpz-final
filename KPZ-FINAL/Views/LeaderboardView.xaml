<UserControl x:Class="KPZ_FINAL.Views.LeaderboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:WpfLibrary.models;assembly=WpfLibrary">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🏆 Leaderboard" FontSize="24" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Difficulty: " VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox SelectedValue="{Binding SelectedDifficulty}"
          SelectedValuePath="Tag"
          Width="120">
                <ComboBoxItem Content="UltraEasy"   Tag="{x:Static models:DifficultyLevel.UltraEasy}"/>
                <ComboBoxItem Content="Easy"   Tag="{x:Static models:DifficultyLevel.Easy}"/>
                <ComboBoxItem Content="Medium" Tag="{x:Static models:DifficultyLevel.Medium}"/>
                <ComboBoxItem Content="Hard"   Tag="{x:Static models:DifficultyLevel.Hard}"/>
                <ComboBoxItem Content="Custom" Tag="{x:Static models:DifficultyLevel.Custom}"/>
            </ComboBox>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Records}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="Transparent" RowBackground="{StaticResource SurfaceBrush}"
                  AlternatingRowBackground="{StaticResource BackgroundBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"           Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource RowIndexConverter}}" Width="40"/>
                <DataGridTextColumn Header="Player"      Binding="{Binding PlayerName}"    Width="*"/>
                <DataGridTextColumn Header="Time"        Binding="{Binding FormattedTime}" Width="80"/>
                <DataGridTextColumn Header="Difficulty"  Binding="{Binding Difficulty}"    Width="90"/>
                <DataGridTextColumn Header="Date"        Binding="{Binding FormattedDate}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Clear Records" Command="{Binding ClearCommand}"
                Style="{StaticResource DangerButtonStyle}"
                HorizontalAlignment="Right" Margin="0,12,0,0"/>
    </Grid>
</UserControl>
